
defaults:
  compareMd5sOnCopy: false
  ssh:
    username: root
    password: root

commands:
  default:
    scripts:
    - ./start.sh

roles:
  all:
    hosts:
    - localhost
    - stampy-host1

data:
  template1-message: Hi from template1
  template2-message: Hi from template2
