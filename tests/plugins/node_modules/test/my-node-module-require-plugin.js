"use strict";

class MyNodeModuleRequirePlugin {
    expandRequires(ctx, args) {
        ctx.log('STDOUT', `my-node-module-require ${args.join(' ')}`);
        return Promise.resolve([]);
    }
}

module.exports = MyNodeModuleRequirePlugin;
