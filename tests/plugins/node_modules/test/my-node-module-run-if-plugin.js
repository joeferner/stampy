"use strict";

class MyNodeModuleRunIfPlugin {
    shouldExecute(ctx, script, args) {
        ctx.logWithScript(script, 'STDOUT', `my-node-module-run-if ${args.join(' ')}`);
        return Promise.resolve(true);
    }
}

module.exports = MyNodeModuleRunIfPlugin;
